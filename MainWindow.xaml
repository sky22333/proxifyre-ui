<ui:FluentWindow x:Class="proxifyre_ui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:tray="clr-namespace:Wpf.Ui.Tray.Controls;assembly=Wpf.Ui.Tray"
        xmlns:local="clr-namespace:proxifyre_ui"
        Title="ProxiFyre 配置工具" Height="550" Width="700"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        Icon="pack://application:,,,/Resources/icons-network.ico"
        Closing="FluentWindow_Closing">

    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>

    <Grid>
        <ContentPresenter x:Name="RootContentDialogPresenter" Grid.Row="0" Grid.RowSpan="2" Panel.ZIndex="99" />
        <tray:NotifyIcon
            x:Name="TrayIcon"
            TooltipText="ProxiFyre 托盘工具"
            Icon="pack://application:,,,/Resources/icons-network.ico"
            FocusOnLeftClick="True"
            MenuOnRightClick="True"
            LeftClick="TrayIcon_LeftClick">
            <tray:NotifyIcon.Menu>
                <ContextMenu>
                    <MenuItem Header="显示主窗口" Click="TrayIcon_LeftClick">
                        <MenuItem.Icon>
                            <ui:SymbolIcon Symbol="WindowApps24" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator />
                    <MenuItem Header="退出程序" Click="TrayMenu_ExitApp_Click">
                        <MenuItem.Icon>
                            <ui:SymbolIcon Symbol="Power24" Foreground="Red" />
                        </MenuItem.Icon>
                    </MenuItem>
                </ContextMenu>
            </tray:NotifyIcon.Menu>
        </tray:NotifyIcon>

        <ui:TitleBar Title="ProxiFyre 配置工具" Grid.Row="0" Margin="0,0,0,10" />

        <Grid Margin="15,40,15,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" />
                <ColumnDefinition Width="15" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Column="0" Grid.Row="0" Grid.RowSpan="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="代理列表" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,5" />
                
                <ListView Grid.Row="1" ItemsSource="{Binding Proxies}" SelectedItem="{Binding SelectedProxy}" 
                          BorderThickness="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                          Margin="0,0,0,10">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayName}" Padding="5" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <ui:Button Icon="{ui:SymbolIcon Add24}" Content="添加" Command="{Binding AddProxyCommand}" Margin="0,0,10,0" />
                    <ui:Button Icon="{ui:SymbolIcon Delete24}" Content="删除" Command="{Binding RemoveProxyCommand}" />
                </StackPanel>
            </Grid>

            <Grid Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" DataContext="{Binding SelectedProxy}" IsEnabled="{Binding Converter={x:Static local:NullToBooleanConverter.Instance}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Text="节点配置" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,5" />

                <StackPanel Grid.Row="1" Margin="0,0,0,10">
                    <Grid Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="socks代理地址" Margin="0,0,0,2" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBox Text="{Binding Ip, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="例如: 127.0.0.1" />
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="端口" Margin="0,0,0,2" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBox Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="10808" />
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="用户名 (可选)" Margin="0,0,0,2" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="密码 (可选)" Margin="0,0,0,2" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            <ui:PasswordBox Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </Grid>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <TextBlock Text="支持协议：" VerticalAlignment="Center" Margin="0,0,15,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                        <CheckBox Content="TCP" IsChecked="{Binding IsTcp}" Margin="0,0,15,0" />
                        <CheckBox Content="UDP" IsChecked="{Binding IsUdp}" />
                    </StackPanel>
                </StackPanel>

                <TextBlock Grid.Row="2" Text="代理应用" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,5" />
                
                <Grid Grid.Row="3">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <ListView Grid.Row="0" ItemsSource="{Binding AppNames}" x:Name="AppsList"
                              BorderThickness="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                              Margin="0,0,0,10"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding}" />
                                    <ui:Button Grid.Column="1" Icon="{ui:SymbolIcon Delete24}" Appearance="Transparent" Foreground="Red"
                                               Command="{Binding DataContext.RemoveAppCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                               CommandParameter="{Binding}" Padding="4,2" Margin="5,0,0,0" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ui:TextBox Grid.Column="0" 
                                    Text="{Binding DataContext.ManualAppInput, RelativeSource={RelativeSource AncestorType=Window}, UpdateSourceTrigger=PropertyChanged}" 
                                    PlaceholderText="输入或粘贴名称/路径" 
                                    ClearButtonEnabled="True" />
                        <ui:Button Grid.Column="2" Content="添加" Icon="{ui:SymbolIcon Add24}"
                                   Command="{Binding DataContext.AddManualAppCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
                        <ui:Button Grid.Column="4" Content="浏览..." Icon="{ui:SymbolIcon FolderOpen24}"
                                   Command="{Binding DataContext.AddAppCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
                    </Grid>
                </Grid>
            </Grid>

            <Grid Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="2" Margin="0,15,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="100" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <ui:Button Grid.Column="1" Width="120" Height="36" 
                               Command="{Binding ToggleStartCommand}" 
                               Appearance="{Binding IsRunning, Converter={x:Static local:RunningToAppearanceConverter.Instance}}">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="{Binding IsRunning, Converter={x:Static local:RunningToIconConverter.Instance}}" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding IsRunning, Converter={x:Static local:RunningToTextConverter.Instance}}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ui:Button>
                </Grid>

                <ListBox x:Name="LogListBox" Grid.Row="1" ItemsSource="{Binding LogLines}" 
                         VirtualizingStackPanel.IsVirtualizing="True"
                         VirtualizingStackPanel.VirtualizationMode="Recycling"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         Background="{DynamicResource LayerOnMicaBaseAltFillColorDefaultBrush}"
                         FontFamily="Consolas" FontSize="12"
                         BorderThickness="0" Padding="5">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="Padding" Value="0,2"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Grid>
    </Grid>
</ui:FluentWindow>
